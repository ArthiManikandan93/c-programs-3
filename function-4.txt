#include <stdio.h>

void circle(float);   

int main()
{
    float r;

    scanf("%f", &r);
    circle(r);
    
    return 0;
}

void circle(float r)  
{
    float area;

    area = 3.14 * r * r;

    printf("%.2f", area);
}